---
hosts_hostname: "{{ ansible_hostname }}"
hosts_domain: "{{ ansible_domain }}"
hosts_ipv4: "{{ ansible_default_ipv4.address }}"
hosts_ipv6: "{{ ansible_default_ipv6.address | default('') }}"
hosts_alias: []
hosts_entrys: []
# hosts_entrys:
#  - hostname: dc
#    ip: 192.168.178.20
#    domain: onkeldom.local
#    comment: "TEST DING"
#    alias: ['adc', 'idc']
